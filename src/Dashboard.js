import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title="Proyecto 1 - Bases 2" />
        <CardContent>Consulta de modulos y datos estadisticos sobre la base de datos WideWo</CardContent>
    </Card>
);